# docs
Documentation for skapersok.

## Run locally
Create a local virtual environment and install dependencies:

```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

Then run the following command to start a local server with live reloading:
```
mkdocs serve --livereload
```