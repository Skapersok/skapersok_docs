# System
Skapersøk is a hirarchical inventory system, meaning that your inventory is structured kind of like the folders on you computer, except that instead of folders and files, we have only one datatype: the crate.

The crate both cointains information about it self, and can contain other crates.